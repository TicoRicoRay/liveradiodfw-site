# Session Wrap — 2026-04-20 PM

## What shipped this session (gh-pages + docs)

### gh-pages (live site)
| Change | Commit |
|---|---|
| Past-shows #18: Lava Cantina rewrite (removed seasonal phrasing) | [4f65317](https://github.com/TicoRicoRay/liveradiodfw-site/commit/4f65317) |
| B17: "CDT" → "Central" in sync alert email copy | [4cabd7c](https://github.com/TicoRicoRay/liveradiodfw-site/commit/4cabd7c) |
| B19: `validFrom` added to Offer schema (GSC fix) | [ea37eae](https://github.com/TicoRicoRay/liveradiodfw-site/commit/ea37eae) |
| B12: theme persists across pages (localStorage + mobile-toggle fix) | [f3bba3d](https://github.com/TicoRicoRay/liveradiodfw-site/commit/f3bba3d) |

### docs branch
| Change | Commit |
|---|---|
| Wrap notes (AM session) | [5b9e9ec](https://github.com/TicoRicoRay/liveradiodfw-site/commit/5b9e9ec) |
| B18/B19 filing | [c7ac58c](https://github.com/TicoRicoRay/liveradiodfw-site/commit/c7ac58c) |
| B12 resolution | [5ef318f](https://github.com/TicoRicoRay/liveradiodfw-site/commit/5ef318f), [a04d8a3](https://github.com/TicoRicoRay/liveradiodfw-site/commit/a04d8a3) |
| B6 close + B7 clarification | [445eee1](https://github.com/TicoRicoRay/liveradiodfw-site/commit/445eee1) |
| B16 close + B16.2 open + B20 file | [109db6b](https://github.com/TicoRicoRay/liveradiodfw-site/commit/109db6b) |

## Bug list state

### Closed this session
B6, B12, B16 (Stage 1), B17, B18, B19 — plus Lava Cantina past-show rewrite.

### Open
- **B1** — Calendar sync cron drifts across DST (pairs with B7 Part 2)
- **B7** — Webhook passphrase + URL publicly readable; relocate `sync_calendar.py` off gh-pages **(TOP PRIORITY)**
- **B9** — Availability script not runnable from Jarvis sandbox
- **B16.2** — Stage 2 auto-publish trigger (waiting on 3-5 natural sync adds)
- **B20** — 22 legacy Bandzoogle-era past-show descriptions miss v2 voice (drafts ready for Ray's approval — see below)

## B20 rewrites — ready for rapid-fire approval

All 22 legacy descriptions have been rewritten in v2 warm-invitation voice, DRAFT-prefixed, and passed all cardinal-rule gates (no exclamation points, no em-dashes, no "dive"/"cover band"/"CDT" phrases, length 300-530 chars, venue + city grounded).

**Artifacts in `/home/user/workspace/lrdfw-past-shows/`:**
- `b20_rewrites.json` — source of truth for the 22 drafts
- `b20_punchlist.html` — rapid-fire review view (shared with Ray this session as `past_shows_punchlist`)
- `b20_stragglers_audit.txt` — original audit with current text for reference

**Next-session workflow:**
1. Ray reviews `b20_punchlist.html` rapid-fire, marks any that need edits
2. For each approved draft, strip `[DRAFT ...]` prefix
3. Merge into `shows.json` via `shows_patch.py` (strict mode — will refuse if any prefixes remain)
4. Run `build_show_pages.py` + `build_includes.py`
5. Commit + push to gh-pages: `user.name="Ray Myers (via Jarvis)"`, `user.email="info@liveradiodfw.com"`, `api_credentials=['github']`

**Special notes on specific drafts:**
- **2025-08-23 Harvest Hall** — the current text is already v2-quality (411 chars, good venue-specific prose). Only issue was 2 exclamation points. The proposed rewrite is essentially the same text with "!" → "." and the "This venue rocks!" opener removed.
- **2024-08-09 Harvest Hall** — preserves the 80s tribute-show theme from the original (synth-pop, hair metal, power ballads) since that's what this show actually was.
- **2025-10-18 Lucy's Yard** — the only venue with no prior v2 reference. Built from the current description's venue details (concession-style ordering, dog-friendly patios, on-leash park) plus its Watters Creek Village location. Worth Ray's closer read.
- **3 FRESH by Brookshire's entries** (2025-04-05, 2025-05-24, 2025-07-12) that were previously identical — now three distinct descriptions with different seasonal hooks (spring / Memorial Day / mid-summer) so chronological browsers see variation.

## Key context to preserve for next thread

### Cardinal rules (verbatim)
- **Strict Account Separation:** For all Live Radio DFW band work, use ONLY Outlook with info@LiveRadioDFW.com. Never reference or search EOS Worldwide account/calendar/drive.
- **Always say "Central" or "America/Chicago"** — never CDT/CST
- **No em-dashes in prose.** `&mdash;` HTML entity acceptable site convention.
- **No "15 minutes" estimates**
- **NEVER "dive" or "dive bar"** — Use "neighborhood bar," "local live-music spot," "locally owned bar."
- **Band longevity:** Musicians together since 2021. "Live Radio DFW" name is post-merger. NEVER "formed in 2026."

### Calendar architecture (post-2026-04-17)
- **GCal is sole source of truth** for shows (info@liveradiodfw.com, free Google personal account)
- **Outlook decommissioned for calendar** (2026-04-17) but **still used for email transport** — `sync_calendar.py:488` still uses the Outlook connector to send alert emails, which is fine per Ray 2026-04-20 PM ("not a problem - we can use Outlook for email - just not for calendar")
- `sync_calendar.py` runs daily via Perplexity cron (set up in prior thread; `schedule_cron action=list` returns empty here — per-thread blind spot, but the cron still runs)
- Library exports from `sync_calendar.py` imported by 6 callers, so the script cannot be deleted — only relocated (B7 Part 2)

### Build pipeline (never forget)
Full show-page rebuild requires BOTH steps:
1. `python3 /tmp/lrdfw-lander/build_show_pages.py`
2. `python3 /tmp/lrdfw-lander/build_includes.py` (nav/footer injection — CRITICAL)
3. git commit + push to gh-pages

### Diff-scope sanity check (from B18 lesson)
Before `git add` on any shows-related commit: `git status --short | wc -l` should match intended change size. If you're rewriting 1 description, expect ~1-2 files changed. If you see 95, STOP — something regressed.

### Repos
- `/tmp/lrdfw-lander/` — gh-pages branch clone (live site)
- `/tmp/lrdfw-wrap/` — docs branch clone (wrap documentation)

### Workspace files preserved
- `/home/user/workspace/lrdfw-past-shows/b20_rewrites.json` — 22 v2-voice rewrites ready for approval
- `/home/user/workspace/lrdfw-past-shows/b20_punchlist.html` — rapid-fire review HTML
- `/home/user/workspace/lrdfw-past-shows/b20_stragglers_audit.txt` — original audit
- `/home/user/workspace/lrdfw-past-shows/shows_patch.py` — hardened (strict mode default, strip-on-write always)
- `/home/user/workspace/lrdfw-past-shows/drafts.json` — 65 clean v2 descriptions (prior batch)

### Connectors confirmed
- github_mcp_direct (CONNECTED) — use `api_credentials=['github']`
- google_search_console__pipedream (CONNECTED)
- outlook (CONNECTED) — calendar decommissioned, email transport still active

## Session handoff summary
Five bugs closed, one filed with drafts pre-built, comprehensive docs pushed. Total commits: 5 on gh-pages, 5 on docs. Next thread: Ray pastes startup phrase, reviews `b20_punchlist.html`, approves rapid-fire, Jarvis runs the merge + build + push pipeline.
